// Main entry point for the UI

document.addEventListener('DOMContentLoaded', () => {
  const app = document.getElementById('app')
  if (app) {
    app.innerHTML = `
      <h1>SEU Scrubbing Visualizer</h1>
      <p>Scaffold complete! M1 ready for testing.</p>
    `
  }
})
